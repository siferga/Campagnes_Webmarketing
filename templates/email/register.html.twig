<h1>Activez votre compte</h1>
<p>Bonjour
	{{ user.firstname }},</p>
<p>Merci d'activer votre compte ici :</p>
<p>
	<a href="{{ absolute_url(path('verify_user', {token: token})) }}">{{ absolute_url(path('verify_user', {token: token})) }}</a>
</p>
<p>lien expirera dans 30 minutes</p>
